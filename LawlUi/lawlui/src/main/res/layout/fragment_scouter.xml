<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:id="@+id/scouter_layout">

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/scouter_text"
                android:layout_width="wrap_content"
                android:layout_height="fill_parent"
                android:text="@string/scouter_fragment"
                android:inputType="textVisiblePassword"
                android:textStyle="bold"
                android:textSize="15dp"
                android:layout_alignParentTop="true"
                android:layout_alignParentLeft="true"
                android:layout_alignLeft="@+id/add_field_button"/>
            <ImageButton
                android:layout_width="40dp"
                android:layout_height="60dp"
                android:layout_toLeftOf="@+id/ScouterSubmit"
                android:src="@android:drawable/ic_input_add"
                android:id="@+id/add_field_button"/>
            <Button
                android:layout_width="wrap_content"
                android:layout_height="60dp"
                android:text="Submit"
                android:id="@+id/ScouterSubmit"
                android:layout_alignParentRight="true"
                android:layout_alignParentTop="true"
                />
        </RelativeLayout>

        </ScrollView>




    <ProgressBar
        android:id="@+id/progress_bar"
        style="@style/GenericProgressIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"/>

</LinearLayout>
